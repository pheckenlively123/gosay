---
phase: 01-first-runnable
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - internal/cowsay/cows/gopher.cow
  - internal/cowsay/cows/NOTICE
  - internal/cowsay/golden_test.go
  - internal/cowsay/testdata/golden/gopher_say_hello.golden
  - internal/cowsay/testdata/golden/gopher_say_multiline.golden
  - internal/cowsay/testdata/golden/default_say_hello.golden
  - internal/cowsay/testdata/golden/dragon_and_cow_say_hello.golden
  - internal/cowsay/testdata/golden/three_eyes_say_hello.golden
  - internal/cowsay/testdata/golden/non_eoc_say_hello.golden
  - cmd/gosay/main.go
  - go.mod
  - go.sum
autonomous: false
requirements:
  - INPUT-01
  - COW-01
  - COWS-04
  - RENDER-01
  - RENDER-02
  - RENDER-03
  - RENDER-04
must_haves:
  truths:
    - "`gosay \"hello\"` prints a gopher ASCII figure with a `< hello >` speech bubble - no panics, no garbled output (Phase 1 Goal #1)"
    - "`gopher.cow` exists as a hand-authored file in `internal/cowsay/cows/`; it is the default animal (Phase 1 Goal #2)"
    - "Golden tests cover the gopher + at least 4 pitfall-targeted cows (default for backslash escape, three-eyes for $eyes manipulation divergence, dragon-and-cow for `\\@` unescape, non-eoc synthetic for dynamic terminator) + a skipped CJK test documenting the Phase 3 gap"
    - "Multi-line input renders with `/ | \\` borders; single-line with `< >` (Phase 1 Goal #4)"
    - "cows/NOTICE includes an attribution paragraph for the hand-authored gopher.cow"
    - "`cmd/gosay/main.go` dispatches to `cowsay.Render(\"gopher\", message, cowsay.RenderOpts{})` and prints the result to stdout"
  artifacts:
    - path: "internal/cowsay/cows/gopher.cow"
      provides: "Hand-authored default cow - the Go gopher, user-selected from 2-3 ASCII variants"
      contains: "$eyes"
      min_lines: 12
    - path: "internal/cowsay/cows/NOTICE"
      provides: "Updated to include gopher.cow attribution paragraph (was created by Plan 01-02 with the placeholder)"
      contains: "gopher.cow"
    - path: "internal/cowsay/golden_test.go"
      provides: "Goldie-driven golden tests + skipped CJK test"
      contains: "goldie.New"
    - path: "internal/cowsay/testdata/golden/gopher_say_hello.golden"
      provides: "Expected output for `Render(\"gopher\", \"hello\", RenderOpts{})`"
    - path: "internal/cowsay/testdata/golden/gopher_say_multiline.golden"
      provides: "Expected output for `Render(\"gopher\", \"line1\\nline2\", RenderOpts{})`"
    - path: "internal/cowsay/testdata/golden/default_say_hello.golden"
      provides: "Expected output for `Render(\"default\", \"hello\", RenderOpts{})` - backslash unescape regression check"
    - path: "internal/cowsay/testdata/golden/dragon_and_cow_say_hello.golden"
      provides: "Expected output exercising the `\\@` -> `@` unescape path"
    - path: "internal/cowsay/testdata/golden/three_eyes_say_hello.golden"
      provides: "Documents gosay's two-eye output for three-eyes.cow (the Perl chop() manipulation is not replicated)"
    - path: "internal/cowsay/testdata/golden/non_eoc_say_hello.golden"
      provides: "Expected output exercising the non-EOC dynamic-terminator path via the synthetic fixture"
    - path: "cmd/gosay/main.go"
      provides: "Wired entry point - reads os.Args[1:], joins, dispatches to cowsay.Render(\"gopher\", message, RenderOpts{}), prints to stdout"
      contains: "cowsay.Render"
    - path: "go.mod"
      provides: "Includes github.com/sebdah/goldie/v2 v2.8.0 as a test dependency"
      contains: "sebdah/goldie/v2 v2.8.0"
  key_links:
    - from: "cmd/gosay/main.go"
      to: "internal/cowsay.Render"
      via: "function call after os.Args[1:] join"
      pattern: "cowsay\\.Render\\("
    - from: "internal/cowsay/golden_test.go"
      to: "internal/cowsay/testdata/golden/*.golden"
      via: "goldie.New(t, goldie.WithFixtureDir(\"testdata/golden\")).Assert"
      pattern: "goldie\\.New|WithFixtureDir"
    - from: "internal/cowsay/cows/gopher.cow"
      to: "internal/cowsay/embed.go cowFS"
      via: "//go:embed cows/*.cow glob captures the new file automatically"
      pattern: "gopher\\.cow"
---

<objective>
Close the phase: hand-author gopher.cow with the user-pick gate, wire main.go to call the real renderer, and lock in golden-file regression coverage for the gopher plus 4 pitfall-targeted cows (default, dragon-and-cow, three-eyes, non-eoc synthetic), with a skipped CJK golden documenting the Phase 3 displayWidth gap.

Purpose: This is the plan that flips the binary from "walking skeleton stub" (Plan 01-01) to "actually renders a gopher saying hello with a correct speech bubble" (Phase 1 Goal #1). After this plan all four Phase 1 success criteria from ROADMAP.md hold.

Output: gopher.cow (user-selected), NOTICE updated, golden_test.go with goldie wiring, 6 .golden fixture files (5 active + 1 skipped CJK), main.go updated to dispatch to cowsay.Render, go.mod updated with the goldie dependency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/work/gosay/CLAUDE.md
@/work/gosay/.planning/phases/01-first-runnable/01-CONTEXT.md
@/work/gosay/.planning/phases/01-first-runnable/01-RESEARCH.md
@/work/gosay/.planning/phases/01-first-runnable/SKELETON.md
@/work/gosay/.planning/phases/01-first-runnable/01-01-PLAN.md
@/work/gosay/.planning/phases/01-first-runnable/01-02-PLAN.md
@/work/gosay/.planning/phases/01-first-runnable/01-03-PLAN.md
</context>

<interfaces>
This plan consumes interfaces created by Plans 01-02 and 01-03:

From internal/cowsay/embed.go (Plan 01-02):
- The //go:embed cows/*.cow glob picks up gopher.cow automatically once the file lands in internal/cowsay/cows/. No edit to embed.go is needed.

From internal/cowsay/renderer.go (Plan 01-03):
- type RenderOpts struct { Eyes string; Tongue string; Thoughts string }
- func Render(animal, message string, opts RenderOpts) (string, error)

From cmd/gosay/main.go (Plan 01-01, current state):
- main() reads os.Args[1:], joins with spaces, prints a stub line. This plan replaces the stub-print with a call to cowsay.Render and the import list grows to include github.com/pheckenlively/gosay/internal/cowsay.

Goldie API (per RESEARCH section F-Q16, github.com/sebdah/goldie/v2 v2.8.0):
- import "github.com/sebdah/goldie/v2"
- g := goldie.New(t, goldie.WithFixtureDir("testdata/golden"))
- g.Assert(t, "fixture_name", []byte(actual))
- Regenerate fixtures with: go test ./internal/cowsay -update
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Draft 2-3 gopher.cow ASCII variants and let the user pick (CONTEXT D-04)</name>
  <files>internal/cowsay/cows/gopher.cow (final commit happens after user pick), internal/cowsay/cows/NOTICE (append gopher attribution paragraph)</files>
  <read_first>
    - /work/gosay/.planning/phases/01-first-runnable/01-CONTEXT.md (D-01..D-04: gopher art constraints - detailed ~12+ lines, standing forward-facing, standard $eyes/$tongue placeholders, user-pick gate is non-negotiable)
    - /work/gosay/.planning/phases/01-first-runnable/01-RESEARCH.md section E (existing ASCII gopher renderings as reference: Neo-cowsay gopher.cow, adamryman gophersay, GitHub gist - all under licenses gosay should NOT directly copy; use as inspiration only, hand-author original art)
    - /work/gosay/.planning/phases/01-first-runnable/01-RESEARCH.md section E-Q15 (target dimensions: 12-16 lines, 30-40 chars wide, 2 $thoughts lines at top, $eyes once in face, $tongue once below mouth)
  </read_first>
  <what-built>
    The executor will draft 2-3 ASCII gopher candidates and present them to the user for selection. Each variant is a full .cow file (Perl heredoc shape: `$the_cow = <<EOC; ... EOC`) with $thoughts/$eyes/$tongue placeholders in the standard cowsay positions. Per CONTEXT D-04 and the deep_work_rules in the planning context, this checkpoint is REQUIRED - the executor must not silently commit a single variant.

    Variant guidance (per RESEARCH E-Q15 and the user constraint that this is "the project's signature feature"):
    - Variant 1: Close to the Neo-cowsay reference (familiar gopher shape - 12-16 lines, ~38 chars wide, oval body, two front teeth, two arms visible). Hand-authored, not copied byte-for-byte from Neo-cowsay.
    - Variant 2: More stylized / original - e.g., chunkier proportions, different arm/leg posture, exaggerated buck teeth. Still forward-facing, still standard placeholder positions.
    - Variant 3 (optional, executor's discretion): A more minimal version (10-12 lines) for users who want a smaller footprint. If the first two variants already span the design space, skip this one.

    Each variant MUST use the standard .cow heredoc structure: `##` comment header, `$the_cow = <<EOC;`, 2 lines of `$thoughts` connector at the top, the body with one `$eyes` placeholder in the face and one `$tongue` placeholder below the mouth, then `EOC` terminator. Lines should be LF-terminated.

    After the user picks (e.g., "variant 2"), the executor writes that variant's content to `internal/cowsay/cows/gopher.cow`. The other variants are discarded (not committed).

    Then update `internal/cowsay/cows/NOTICE` (created in Plan 01-02): append a final paragraph specifically for `gopher.cow`. Suggested wording: "`gopher.cow` is hand-authored original ASCII art created for the gosay project by the project authors. The Go gopher mascot was designed by Renee French and is licensed by Google under CC BY 3.0; this ASCII rendering does not include any of Google's gopher artwork and is original work derived from the visual concept. It is distributed under the gosay project LICENSE (root of repo)."

    The placeholder positions in the chosen gopher.cow must be reachable by Plan 01-04's golden tests: `Render("gopher", "hello", RenderOpts{})` should substitute Eyes="oo" and Tongue="  " (defaults) into the gopher art and produce the expected golden output. The executor regenerates the golden file in Task 4 after this checkpoint resolves.
  </what-built>
  <how-to-verify>
    The executor presents the 2-3 variants inline in chat (as code blocks showing the rendered output of `cowsay.Render("gopher_variant_N", "hello", RenderOpts{})` for each candidate, OR just the raw .cow file content with a note explaining how the eyes/tongue would substitute). For each variant, show:
      1. The raw .cow file content (between $the_cow = <<EOC; and EOC, with placeholders visible)
      2. The rendered output for the input "hello" (executor can build a temporary helper or hand-render with eyes="oo" and tongue="  " for the preview)
      3. A short caption (1-2 sentences) describing the design choices

    The user verifies by:
      1. Looking at each variant's rendered preview
      2. Confirming the gopher is recognizable as the Go mascot (D-01)
      3. Confirming it is standing and facing forward (D-02)
      4. Confirming the buck-teeth area is distinct from the eye area (D-03 - $tongue and $eyes substitute in different positions)
      5. Picking ONE variant by name or number (e.g., "variant 2" or "the chunky one")

    If none of the variants are acceptable, the user says so and the executor produces another round (this is rare - most projects converge in one pass per CONTEXT.md "Specific Ideas").

    After the pick, the executor writes the final gopher.cow file and updates NOTICE. The executor then continues to Task 2 without waiting for further input.
  </how-to-verify>
  <resume-signal>
    User types the variant choice (e.g., `variant 2`, `the second one`, `chunky one`) OR describes refinements needed (e.g., `closer to variant 1 but with longer arms`).
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire main.go to cowsay.Render and add goldie dependency</name>
  <files>cmd/gosay/main.go, go.mod, go.sum</files>
  <read_first>
    - /work/gosay/.planning/phases/01-first-runnable/01-CONTEXT.md (Phase 1 main.go input model: os.Args[1:], hardcoded "gopher", cowsay.Render dispatch)
    - /work/gosay/.planning/phases/01-first-runnable/01-RESEARCH.md section G-Q18 (exact main.go body for Phase 1)
    - /work/gosay/.planning/phases/01-first-runnable/01-RESEARCH.md section F-Q17 (goldie dependency: github.com/sebdah/goldie/v2 v2.8.0)
    - /work/gosay/.planning/phases/01-first-runnable/01-01-PLAN.md (current main.go state - prints a "would render" stub line)
    - /work/gosay/.planning/phases/01-first-runnable/01-RESEARCH.md "Package Legitimacy Audit" (goldie/v2 is approved - 7 years old, widely used, single dependency)
  </read_first>
  <action>
    Replace the body of cmd/gosay/main.go (currently a walking-skeleton stub from Plan 01-01) with the production wiring per RESEARCH section G-Q18 and CONTEXT.md Phase 1 main.go input model:

    Package: main. Imports: fmt, os, strings, github.com/pheckenlively/gosay/internal/cowsay.

    func main() body:
    1. If len(os.Args) < 2: fmt.Fprintln(os.Stderr, "usage: gosay <message>") and os.Exit(1).
    2. message := strings.Join(os.Args[1:], " ").
    3. out, err := cowsay.Render("gopher", message, cowsay.RenderOpts{}).
    4. If err != nil: fmt.Fprintln(os.Stderr, err) and os.Exit(1).
    5. fmt.Print(out).

    Do NOT introduce flag.Parse() (Phase 2 / INPUT-02 boundary). Do NOT read stdin (Phase 2). Do NOT add a -h/help branch (Phase 3 / HELP-01). The signature and body must match the spec in RESEARCH G-Q18 exactly.

    Then add the goldie dependency to go.mod. Run: `go get github.com/sebdah/goldie/v2@v2.8.0`. This will update both go.mod (adding a `require github.com/sebdah/goldie/v2 v2.8.0` line and an `// indirect` block for goldie's transitive deps) and go.sum (adding checksums). Both files are then committed.

    Confirm the dependency is at the exact pinned version v2.8.0 (per RESEARCH F-Q16 and the Package Legitimacy Audit), not a newer one. If `go get` brings in v2.8.x with a patch bump, that is acceptable; if it brings in v3.x, stop and treat as a research gap (do not silently upgrade).
  </action>
  <verify>
    <automated>cd /work/gosay && grep -q "cowsay\.Render" cmd/gosay/main.go && grep -q "github\.com/pheckenlively/gosay/internal/cowsay" cmd/gosay/main.go && ! grep -q "flag\.Parse" cmd/gosay/main.go && ! grep -q "os\.Stdin" cmd/gosay/main.go && grep -q "github\.com/sebdah/goldie/v2 v2\.8\." go.mod && go build ./cmd/gosay && rm -f gosay && go vet ./... ; echo "exit=$?"</automated>
  </verify>
  <done>
    cmd/gosay/main.go imports the internal/cowsay package and dispatches to cowsay.Render("gopher", message, cowsay.RenderOpts{}) with the message built from os.Args[1:]. No flag.Parse, no stdin read. go.mod includes github.com/sebdah/goldie/v2 v2.8.x. go build ./cmd/gosay exits 0. go vet ./... exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: End-to-end smoke - verify gosay "hello" produces the expected output shape</name>
  <files>(no file edits - this task is a pre-goldens smoke that protects against accidental regressions before fixtures are frozen)</files>
  <read_first>
    - /work/gosay/.planning/ROADMAP.md (Phase 1 success criteria - especially #1: `gosay "hello"` prints a gopher with `< hello >` speech bubble)
    - /work/gosay/.planning/phases/01-first-runnable/01-CONTEXT.md (Phase Boundary)
  </read_first>
  <action>
    With Tasks 1 and 2 complete (gopher.cow exists, main.go wired, goldie added), run a manual smoke before freezing goldens:

    1. `go build -o /tmp/gosay ./cmd/gosay`
    2. `/tmp/gosay "hello"` -> capture stdout to a temp file `/tmp/gosay_hello.out`
    3. Inspect /tmp/gosay_hello.out: it MUST contain the literal substring `< hello >` (the speech bubble middle line) AND visible gopher ASCII art on subsequent lines. Verify by `grep -q "< hello >" /tmp/gosay_hello.out` and `grep -c '^' /tmp/gosay_hello.out` is at least 12 (3 balloon lines + 12+ gopher lines minimum).
    4. `/tmp/gosay "line one" "line two"` -> stdout joins the args with a single space, producing a single-line bubble containing `< line one line two >` (not multi-line - multi-line requires an actual `\n` in input).
    5. `/tmp/gosay $'line1\nline2'` (shell ANSI-C quoting injects a real newline) -> stdout contains `/ line1 \` and `\ line2 /` (multi-line bubble).
    6. `/tmp/gosay` (no args) -> exit 1, stderr contains `usage: gosay <message>`.
    7. Clean up: `rm -f /tmp/gosay /tmp/gosay_hello.out`.

    Do NOT commit /tmp/gosay_hello.out. This task is verification; the golden fixtures in Task 4 are the durable artifacts.

    If any of the assertions in steps 3-6 fail, do NOT proceed to Task 4 - regenerating goldens against broken output would freeze the bugs. Diagnose and fix before continuing. The likely culprits if step 3 fails are: gopher.cow has wrong placeholder names, parser dropped lines, balloon math is off-by-one. Re-read Plan 01-03's tests and the gopher.cow file content.
  </action>
  <verify>
    <automated>cd /work/gosay && go build -o /tmp/gosay_smoke ./cmd/gosay && /tmp/gosay_smoke "hello" > /tmp/gosay_hello_smoke.out && grep -q "< hello >" /tmp/gosay_hello_smoke.out && [ $(wc -l < /tmp/gosay_hello_smoke.out) -ge 12 ] && /tmp/gosay_smoke 2>&1 1>/dev/null | grep -q "usage: gosay" && rm -f /tmp/gosay_smoke /tmp/gosay_hello_smoke.out ; echo "exit=$?"</automated>
  </verify>
  <done>
    `gosay "hello"` produces stdout containing `< hello >` followed by ≥12 lines of gopher ASCII art. `gosay` (no args) exits non-zero with the usage message on stderr. Multi-line input produces `/ ... \` and `\ ... /` borders. All smoke assertions pass before goldens are frozen.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add golden tests for gopher + 4 pitfall cows + skipped CJK marker</name>
  <files>internal/cowsay/golden_test.go, internal/cowsay/testdata/golden/gopher_say_hello.golden, internal/cowsay/testdata/golden/gopher_say_multiline.golden, internal/cowsay/testdata/golden/default_say_hello.golden, internal/cowsay/testdata/golden/dragon_and_cow_say_hello.golden, internal/cowsay/testdata/golden/three_eyes_say_hello.golden, internal/cowsay/testdata/golden/non_eoc_say_hello.golden</files>
  <read_first>
    - /work/gosay/.planning/phases/01-first-runnable/01-CONTEXT.md (D-11..D-15, D-19: goldie v2.8.0, gopher + 3-5 pitfall cows in testdata/golden/, hand-curated, CJK t.Skip golden)
    - /work/gosay/.planning/phases/01-first-runnable/01-RESEARCH.md section F-Q16 (goldie API, fixture naming convention, WithFixtureDir option, the recommended fixture name list)
    - /work/gosay/.planning/phases/01-first-runnable/01-RESEARCH.md section B-Q4, B-Q5, B-Q6 (pitfall cow selection rationale: default for `\\` unescape, dragon-and-cow for `\@` unescape, three-eyes for $eyes placeholder, non-EOC synthetic for dynamic terminator)
    - /work/gosay/.planning/phases/01-first-runnable/01-03-PLAN.md (the testdata/fixtures/non-eoc.cow file already exists from Plan 01-03 Task 1; this plan reuses it for the non-EOC golden via a small custom-load test helper)
  </read_first>
  <behavior>
    - go test ./internal/cowsay -run TestGoldens passes after `go test ./internal/cowsay -update -run TestGoldens` is run once to seed the .golden files
    - Each golden file in testdata/golden/ contains exactly the output Render produces for the matching (animal, message, opts) tuple
    - TestGolden_CJK_Skipped is present but skipped via t.Skip with a message naming Phase 3 / RENDER-06 / runewidth as the unblocker
    - TestGolden_NonEOC reads internal/cowsay/testdata/fixtures/non-eoc.cow directly (it is not in the embedded cow set), parses it via parseCowBody, builds the balloon for "hello", does substitution with defaults, and compares against non_eoc_say_hello.golden
    - TestListCows passes assert: ListCows() now contains "gopher" (this is a regression from Plan 01-02's `>= 50` floor - now it must be exactly 51)
  </behavior>
  <action>
    Create internal/cowsay/golden_test.go (package cowsay). Imports: bytes, os, testing, github.com/sebdah/goldie/v2.

    Helper at file scope:
    - newGoldie(t *testing.T) *goldie.Goldie: returns goldie.New(t, goldie.WithFixtureDir("testdata/golden")). One call site means a small one-liner is fine; if it stays at a single call site, inline directly instead.

    Test functions (per the fixture naming convention in RESEARCH F-Q16):

    1. TestGolden_GopherSayHello: out, err := Render("gopher", "hello", RenderOpts{}). require no error. g.Assert(t, "gopher_say_hello", []byte(out)).

    2. TestGolden_GopherSayMultiline: out, err := Render("gopher", "line1\nline2", RenderOpts{}). require no error. g.Assert(t, "gopher_say_multiline", []byte(out)).

    3. TestGolden_DefaultSayHello: out, err := Render("default", "hello", RenderOpts{}). require no error. g.Assert(t, "default_say_hello", []byte(out)). This is the canonical backslash-unescape regression: default.cow's tail line `($eyes)\\_______` must render with a single `\`, not `\\`.

    4. TestGolden_DragonAndCowSayHello: out, err := Render("dragon-and-cow", "hello", RenderOpts{}). require no error. g.Assert(t, "dragon_and_cow_say_hello", []byte(out)). This exercises the `\@` -> `@` unescape path (dragon-and-cow.cow's body contains `\@___\@`).

    5. TestGolden_ThreeEyesSayHello: out, err := Render("three-eyes", "hello", RenderOpts{}). require no error. g.Assert(t, "three_eyes_say_hello", []byte(out)). The golden captures gosay's actual two-eye output; a comment block above the test names the divergence ("upstream Perl cowsay's chop() manipulation produces three-character eyes; gosay's strings.NewReplacer substitutes the bare two-character $eyes value, yielding two eyes; this is documented in RESEARCH section B-Q6 and is not a bug").

    6. TestGolden_NonEOCSayHello: this fixture is the synthetic one from Plan 01-03's testdata/fixtures/non-eoc.cow, NOT in the embedded cow set. So Render("non-eoc", ...) would fail. The test does its own pipeline:
       a. data, err := os.ReadFile("testdata/fixtures/non-eoc.cow")
       b. body, err := parseCowBody(data) (same-package access)
       c. balloon := buildBalloon("hello")
       d. r := strings.NewReplacer("$thoughts", "\\", "${thoughts}", "\\", "$eyes", "oo", "${eyes}", "oo", "$tongue", "  ", "${tongue}", "  ")
       e. substituted := r.Replace(body)
       f. out := balloon + substituted
       g. g.Assert(t, "non_eoc_say_hello", []byte(out))
       The point is to prove the parser handles a non-EOC terminator AND the resulting body still passes through balloon + substitution correctly.

    7. TestGolden_CJK_Skipped: starts with t.Skip("CJK display width requires runewidth (Phase 3 / RENDER-06); gosay's Phase 1 displayWidth uses utf8.RuneCountInString which under-pads the balloon for CJK input - this test exists to document the gap, will be enabled in Phase 3"). After t.Skip the rest of the function can be a real assertion using `漢字テスト` (those code points, written as Go escapes for portability), captured in cjk_skip.golden. Do NOT create cjk_skip.golden in this plan since the test is skipped (goldie does not need the file when t.Skip runs before g.Assert). Phase 3 will both un-skip and seed the golden simultaneously via go test -update.

    8. TestListCows_IncludesGopher: a smaller test (NOT under TestGolden_) that asserts ListCows() includes "gopher". Specifically: names, err := ListCows(); require no error; assert len(names) == 51 (was >= 50 in Plan 01-02; the gopher addition tightens this); assert names contains "gopher". This is the regression assertion that gopher.cow actually landed in the embedded set.

    Seeding goldens: run `go test ./internal/cowsay -update -run TestGolden` to generate the .golden files from current Render output. Then commit the .golden files. Re-run `go test ./internal/cowsay -run TestGolden` to confirm the assertions match (no diff).

    IMPORTANT: The user reviews each golden once during this task (per D-13: "the user reviews each golden once during Phase 1 execution"). After seeding, the executor surfaces the contents of each .golden file (or at least the gopher_say_hello.golden which is the keystone) in chat for the user to eyeball before committing. If the user rejects a golden (e.g., spots a rendering bug), the executor diagnoses, fixes, regenerates, and re-presents. This is NOT a separate checkpoint task - it is part of the TDD-style RED -> GREEN -> REVIEW cycle for this task.
  </action>
  <verify>
    <automated>cd /work/gosay && test -f internal/cowsay/golden_test.go && grep -q "goldie\.New" internal/cowsay/golden_test.go && grep -q "WithFixtureDir" internal/cowsay/golden_test.go && grep -q "t\.Skip" internal/cowsay/golden_test.go && ls internal/cowsay/testdata/golden/*.golden | wc -l | grep -qE "^[5-9]$|^[1-9][0-9]+$" && test -f internal/cowsay/testdata/golden/gopher_say_hello.golden && test -f internal/cowsay/testdata/golden/default_say_hello.golden && test -f internal/cowsay/testdata/golden/three_eyes_say_hello.golden && test -f internal/cowsay/testdata/golden/non_eoc_say_hello.golden && go vet ./... && go test ./internal/cowsay -run "TestGolden|TestListCows" -v 2>&1 | tail -30 ; echo "exit=$?"</automated>
  </verify>
  <done>
    internal/cowsay/golden_test.go exists with at least 7 test functions (6 active goldens + 1 skipped CJK). Five .golden files exist in testdata/golden/ (gopher_say_hello, gopher_say_multiline, default_say_hello, dragon_and_cow_say_hello, three_eyes_say_hello, non_eoc_say_hello - that is six files; cjk_skip.golden is intentionally absent because the test skips before asserting). go vet ./... clean. go test ./internal/cowsay -run "TestGolden|TestListCows" exits 0 with all non-skipped tests passing and the CJK test reporting SKIP. ListCows() now returns exactly 51 names including "gopher".
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: End-of-phase human verification - phase goal satisfied</name>
  <what-built>
    All Phase 1 success criteria from ROADMAP.md are now buildable and runnable. The executor has:
      1. Wired the binary end-to-end (Plan 01-01 walking skeleton + Plan 01-04 main wire-up)
      2. Vendored the upstream cow set (Plan 01-02)
      3. Built the rendering engine (Plan 01-03)
      4. Hand-authored gopher.cow with the user's chosen design (Task 1 of this plan)
      5. Locked in golden regression coverage (Task 4)
    This checkpoint surfaces the phase output to the user so they can verify the four ROADMAP success criteria in one pass before the phase is marked complete.
  </what-built>
  <how-to-verify>
    From /work/gosay:
      1. `go build ./...` exits 0
      2. `go test ./...` exits 0 (every internal/cowsay test passes; the CJK golden reports SKIP)
      3. `go vet ./...` exits 0
      4. `go run ./cmd/gosay "hello"` prints the chosen gopher saying `< hello >` with the speech bubble correctly bordered. Inspect visually for: balloon border alignment (top/bottom `_` and `-` counts match max width + 2), `< hello >` middle line, gopher ASCII art rendered with no `$eyes` or `$tongue` placeholders visible (substitution complete), no `\\` double backslashes visible (unescape complete).
      5. `go run ./cmd/gosay "line one" "line two"` produces single-line bubble (args joined with space, no newline).
      6. `go run ./cmd/gosay $'first\nsecond'` (bash ANSI-C quoting) produces multi-line bubble with `/ first  \` (padded) and `\ second /` borders.
      7. `go run ./cmd/gosay` (no args) writes `usage: gosay <message>` to stderr and exits 1.
      8. Spot-check `cat internal/cowsay/cows/NOTICE` - the gopher attribution paragraph is present alongside the daemon.cow provenance paragraph.
      9. Spot-check `cat internal/cowsay/cows/SOURCE.md` - upstream tag v3.8.4 and commit SHA 027c9268ac8571408e153214b9cf1a5e6fab0cfc are present.
      10. Confirm ROADMAP.md Phase 1 success criteria 1-5 each hold:
          - #1: gosay "hello" gopher + speech bubble (step 4 above)
          - #2: gopher.cow exists in cows/ as default (`ls internal/cowsay/cows/gopher.cow`)
          - #3: dynamic terminator + backslash unescape covered by goldens (the non_eoc and default goldens)
          - #4: multi-line `/ | \` vs single-line `< >` (steps 5-6 above)
          - #5: cows/NOTICE + .gitattributes exist (`cat .gitattributes` shows `cows/*.cow text eol=lf`; NOTICE has McKusick paragraph)

    The user signals approval (type "approved" or "phase 1 done") and the phase is marked complete. If any criterion fails, the user describes the issue and the executor diagnoses and fixes before re-requesting approval.
  </how-to-verify>
  <resume-signal>
    User types `approved`, `phase 1 done`, `looks good`, OR describes specific issues to fix (e.g., `bubble border is off by one`, `gopher has typo on line 7`).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input (os.Args) -> Render -> stdout | Positional args flow through the full pipeline to the terminal; this is the load-bearing end-to-end path Plan 01-04 lights up |
| gopher.cow art (user-picked variant) -> embed.FS -> parser -> renderer | A hand-authored file becomes part of the binary; its correctness affects every default invocation |
| go.mod -> module proxy -> goldie/v2 source | Adding the goldie dependency pulls code from the Go module proxy; checksum verification protects against tampering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Tampering | goldie/v2 supply chain | mitigate | github.com/sebdah/goldie/v2 v2.8.0 is approved in RESEARCH "Package Legitimacy Audit" (7-year-old project, widely used, pinned version). Go module proxy checksums (go.sum) are committed alongside go.mod. The legitimacy gate in this plan is closed by the audit table - no blocking checkpoint required (per RESEARCH; goldie/v2 is classified as approved, not [ASSUMED]/[SUS]/[SLOP]). |
| T-01-04-02 | Information disclosure | gopher.cow content visible in committed binary | accept | gopher.cow is hand-authored ASCII art for the project mascot; nothing sensitive embedded. Reviewing the user-picked variant during Task 1 catches accidental ASCII smuggling (URLs, slurs, etc.). |
| T-01-04-03 | Tampering | output injection via cow art with ANSI escapes (especially three-eyes.cow or any upstream cow) | accept | Same disposition as Plan 01-03's T-01-03-03. None of the v3.8.4 upstream cow files contain ANSI escapes (spot-checked in RESEARCH §A); the gopher.cow variants in Task 1 are hand-authored and reviewed by the user. |
| T-01-04-04 | Tampering | golden file drift hiding rendering bugs | mitigate | The Task 4 human-review-of-goldens step (user eyeballs gopher_say_hello.golden before commit per D-13) catches any seeded-bad goldens. Future drift is caught by the goldens themselves on every CI run. |
| T-01-04-05 | Repudiation | gopher.cow attribution missing | mitigate | Task 1 explicitly appends a gopher.cow paragraph to cows/NOTICE; the acceptance criterion `grep -q "gopher\.cow" internal/cowsay/cows/NOTICE` enforces presence. |
| T-01-04-SC | Tampering | npm/pip/cargo/go installs | approved-no-checkpoint | RESEARCH "Package Legitimacy Audit" lists github.com/sebdah/goldie/v2 v2.8.0 with disposition "Approved" - not [ASSUMED], not [SUS], not [SLOP]. Per the planner authority rules, approved packages do not require a blocking-human-verify checkpoint. Go's module checksum (go.sum) provides automatic integrity. |
</threat_model>

<verification>
End-to-end check (run after all five tasks complete):

```
cd /work/gosay
go build ./...                                       # exit 0
go vet ./...                                         # exit 0
go test ./...                                        # exit 0 (CJK test reports SKIP)
go run ./cmd/gosay "hello" | grep -q "< hello >"     # phase goal #1
test -f internal/cowsay/cows/gopher.cow              # phase goal #2
grep -q "gopher" internal/cowsay/cows/NOTICE         # gopher attribution present
ls internal/cowsay/testdata/golden/*.golden | wc -l  # 6 files (gopher x2 + default + dragon-and-cow + three-eyes + non-eoc)
grep -q "027c9268ac8571408e153214b9cf1a5e6fab0cfc" internal/cowsay/cows/SOURCE.md  # provenance
grep -q "Marshall Kirk McKusick" internal/cowsay/cows/NOTICE                       # daemon.cow paragraph
grep -q "cows/\*\.cow text eol=lf" .gitattributes                                  # phase goal #5
```
</verification>

<success_criteria>
- gopher.cow exists in internal/cowsay/cows/ with the user-selected art (Phase 1 Goal #2 / COW-01)
- cows/NOTICE includes both the daemon.cow provenance paragraph (from Plan 01-02) and a gopher.cow attribution paragraph (added by this plan)
- cmd/gosay/main.go imports internal/cowsay and dispatches to cowsay.Render("gopher", message, cowsay.RenderOpts{}); no flag.Parse; no stdin; no -h
- go.mod includes github.com/sebdah/goldie/v2 v2.8.x; go.sum has matching checksums
- 6 .golden fixtures exist (gopher_say_hello, gopher_say_multiline, default_say_hello, dragon_and_cow_say_hello, three_eyes_say_hello, non_eoc_say_hello); cjk_skip.golden is intentionally absent
- go test ./... exits 0; the CJK test reports SKIP with a message naming Phase 3 / RENDER-06
- go run ./cmd/gosay "hello" prints the gopher with `< hello >` (Phase 1 Goal #1 / INPUT-01 / RENDER-04)
- go run ./cmd/gosay $'a\nb' produces `/ a \` and `\ b /` borders (Phase 1 Goal #4 / RENDER-04)
- ListCows() returns exactly 51 names including "gopher"
- The user explicitly approved both the gopher variant pick (Task 1) and the end-of-phase verification (Task 5)
</success_criteria>

<output>
Create .planning/phases/01-first-runnable/01-04-SUMMARY.md when done. Record: which gopher variant the user chose (with the .cow content captured verbatim), the goldie version actually resolved by `go get`, the exact byte count and line count of gopher_say_hello.golden, confirmation that all 7 test functions in golden_test.go ran (6 PASS + 1 SKIP), and the literal output of `go run ./cmd/gosay "hello"` after the phase is complete.
</output>
