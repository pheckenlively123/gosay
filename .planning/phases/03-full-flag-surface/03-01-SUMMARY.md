---
phase: 03-full-flag-surface
plan: "01"
subsystem: rendering
tags: [go-runewidth, unicode, cjk, display-width, balloon, golden-test]

requires:
  - phase: 01-first-runnable
    provides: displayWidth seam in balloon.go (purpose-built in Phase 1 for this exact swap)

provides:
  - runewidth.StringWidth-based displayWidth in balloon.go (D-07)
  - padRight helper replacing all %-*s byte-padding in buildBalloon (D-08)
  - cjk_aligned_gopher.golden fixture asserting correct CJK bubble alignment (D-09)
  - go-runewidth v0.0.24 as a direct dependency in go.mod

affects:
  - 03-02 (wrap plan uses the same displayWidth seam; hardBreak uses utf8.DecodeRuneInString)
  - 03-03 (think-mode plan extends buildBalloon using padRight pattern established here)

tech-stack:
  added:
    - github.com/mattn/go-runewidth v0.0.24 (direct)
    - github.com/clipperhouse/uax29/v2 v2.2.0 (indirect, transitive from runewidth)
  patterns:
    - displayWidth seam: one-line body swap with zero call-site changes
    - padRight(s, targetWidth): display-column padding via strings.Repeat (replaces %-*s)
    - Golden fixture generated via go test -update (never hand-authored)

key-files:
  created:
    - internal/cowsay/testdata/golden/cjk_aligned_gopher.golden
  modified:
    - internal/cowsay/balloon.go
    - internal/cowsay/balloon_test.go
    - internal/cowsay/golden_test.go
    - go.mod
    - go.sum

key-decisions:
  - "D-07: Swap displayWidth body from utf8.RuneCountInString to runewidth.StringWidth — one-line change, zero call-site churn"
  - "D-08: Replace %-*s byte-padding with padRight(s, targetWidth) using display-column arithmetic — both changes required together for CJK alignment"
  - "D-09: Remove t.Skip from CJK golden test and commit real aligned fixture generated by go test -update"
  - "go-runewidth promoted from indirect to direct in go.mod after importing it in balloon.go"

patterns-established:
  - "padRight pattern: s + strings.Repeat(' ', targetWidth-displayWidth(s)) — use for any balloon border padding"
  - "Golden fixture generation: go test -update ./internal/cowsay/ (not go test -update ./...)"

requirements-completed: [RENDER-06]

duration: 18min
completed: 2026-05-31
---

# Phase 3 Plan 01: Unicode Display-Width Fix (RENDER-06) Summary

**runewidth.StringWidth replaces utf8.RuneCountInString in the displayWidth seam, with padRight eliminating all %-*s byte-padding, and cjk_aligned_gopher.golden asserting correct CJK bubble alignment**

## Performance

- **Duration:** 18 min
- **Started:** 2026-05-31T17:00:00Z
- **Completed:** 2026-05-31T17:18:00Z
- **Tasks:** 3
- **Files modified:** 5 (+ 1 new golden fixture)

## Accomplishments
- Added go-runewidth v0.0.24 as a direct dependency (with transitive uax29 v2.2.0 indirect)
- Swapped `displayWidth` body from `utf8.RuneCountInString` to `runewidth.StringWidth` — the seam built in Phase 1 required zero call-site changes
- Added `padRight(s string, targetWidth int) string` helper; replaced all two `fmt.Fprintf("%-*s", ...)` occurrences in `buildBalloon` with `padRight` calls
- De-skipped the CJK golden test and committed an aligned fixture generated by goldie's `-update` flag: `< 漢字テスト >` right border aligns with the 12-wide top/bottom borders

## Task Commits

Each task was committed atomically:

1. **Task 1: Add go-runewidth dependency** - `ea5847c` (chore)
2. **Task 2 RED: Failing test for runewidth displayWidth** - `01835b9` (test)
3. **Task 2 GREEN: Swap displayWidth, add padRight, remove %-*s** - `5404876` (feat)
4. **Task 3: De-skip CJK golden test and commit aligned fixture** - `fe69f81` (feat)

**Plan metadata:** see final commit below

_Note: Task 2 was TDD — separate RED (test) and GREEN (implementation) commits._

## Files Created/Modified
- `/work/gosay/internal/cowsay/balloon.go` - Swapped displayWidth body; added padRight helper; replaced %-*s padding
- `/work/gosay/internal/cowsay/balloon_test.go` - Updated TestDisplayWidth: {"漢字", 2} -> {"漢字", 4}
- `/work/gosay/internal/cowsay/golden_test.go` - Replaced TestGolden_CJK_Skipped (t.Skip) with TestGolden_GopherSayCJK
- `/work/gosay/internal/cowsay/testdata/golden/cjk_aligned_gopher.golden` - NEW: aligned CJK bubble fixture
- `/work/gosay/go.mod` - Added go-runewidth v0.0.24 (direct) + uax29 v2.2.0 (indirect)
- `/work/gosay/go.sum` - Hash entries for both new packages

## Decisions Made
- Kept `buildBalloon` signature as `buildBalloon(message string) string` (signature change deferred to Plan 02 per plan spec)
- go-runewidth is promoted from `// indirect` to direct automatically by `go mod tidy` after the import is added
- Used `go test -update ./internal/cowsay/` (not `./...`) to regenerate goldie fixtures — `./...` fails with "no Go files in ." at the module root

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- `go test -update ./...` fails with "no Go files in /work/gosay" because there are no `.go` files at the module root. Fixed by specifying the package path directly: `go test -update ./internal/cowsay/` — this is the correct invocation; the plan's documented command was a minor documentation issue, not a real problem.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- `displayWidth` seam is ready for Plan 02 (word wrap): `wrapWords`/`hardBreak` will call the same `displayWidth` function established here
- `padRight` pattern is ready for Plan 03 (think mode): `( )` border formatting uses identical padding logic
- All existing tests pass; no regressions introduced

## Known Stubs
None — all changes are complete implementations, not placeholders.

## Threat Flags
None — no new network endpoints, auth paths, file access patterns, or schema changes introduced. The `stdin → displayWidth → runewidth.StringWidth` boundary was evaluated in the plan's threat model (T-03-01, T-03-02, T-03-SC) and all accepted/mitigated as documented.

## Self-Check: PASSED
- `internal/cowsay/balloon.go` exists and contains `runewidth.StringWidth` ✓
- `internal/cowsay/testdata/golden/cjk_aligned_gopher.golden` exists ✓
- `go.mod` contains `github.com/mattn/go-runewidth v0.0.24` (direct) ✓
- `go.mod` contains `github.com/clipperhouse/uax29/v2 v2.2.0` (indirect) ✓
- Commits ea5847c, 01835b9, 5404876, fe69f81 exist in git log ✓
- `grep -c '%-\*s' balloon.go` == 0 ✓
- `go test ./...` passes ✓

---
*Phase: 03-full-flag-surface*
*Completed: 2026-05-31*
