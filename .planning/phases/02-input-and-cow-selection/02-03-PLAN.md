---
phase: 02-input-and-cow-selection
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - cmd/gosay/main.go
  - cmd/gosay/main_test.go
autonomous: true
requirements: [COW-03, COW-04]
must_haves:
  truths:
    - "gosay -l prints a 'Cow files:' header followed by all 51 embedded names in space-separated wrapped columns, exits 0"
    - "gopher appears plain and alphabetical (under g) in the -l listing with no (default) marker"
    - "gosay --random hello prints some embedded animal saying hello, exits 0; the chosen animal is a member of ListCows()"
    - "gosay -f tux --random exits non-zero with a 'cannot combine' error"
    - "gosay -l with a message or with -f/--random exits non-zero (usage error)"
  artifacts:
    - path: "cmd/gosay/main.go"
      provides: "-l columnar listing, --random pool selection, mutual-exclusion guards, extended usage"
      contains: "Cow files:"
    - path: "cmd/gosay/main_test.go"
      provides: "tests for -l alone, -l conflict, --random member, -f+--random conflict"
      contains: "Cow files:"
  key_links:
    - from: "cmd/gosay/main.go run()"
      to: "cowsay.ListCows()"
      via: "source for both -l output and --random pool"
      pattern: "cowsay\\.ListCows\\(\\)"
    - from: "cmd/gosay/main.go run()"
      to: "math/rand"
      via: "rand.Intn over ListCows length"
      pattern: "rand\\.Intn"
---

<objective>
Complete the Phase 2 selection surface on top of the plan-02 flag scaffold: `-l` lists every embedded animal in upstream cowsay columnar format (COW-03 / D-07 / D-08), `--random` picks a random animal from the full 51-cow pool (COW-04 / D-04 / D-05), and the conflicting combinations are hard usage errors (D-06: `-f`+`--random`; D-09: `-l` with a message or selection flag).

This is the final vertical slice of Phase 2: after this plan, every Phase 2 success criterion is observable.

Purpose: Adds discoverability (`-l`) and variety (`--random`) to the working selection path.
Output: Extended `run()` (flags + conflict guards + listing/random branches), new CLI tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-input-and-cow-selection/02-CONTEXT.md
@.planning/phases/02-input-and-cow-selection/02-PATTERNS.md
@.planning/phases/02-input-and-cow-selection/02-02-SUMMARY.md

<interfaces>
<!-- Built on plan 02's flag scaffold + existing cowsay API. -->

From internal/cowsay/embed.go (the source of truth for both -l and --random):
```go
func ListCows() ([]string, error) // sorted, .cow-stripped; 51 names including gopher and daemon
```

From cmd/gosay/main.go (plan 02 — the run() this plan extends; signature unchanged):
```go
func run(args []string, stdout, stderr io.Writer) int // FlagSet built per-call, -f already registered
```
This plan adds `-l` (bool), `--random` (bool), conflict guards, listing and random branches. The `-f` flag, TTY/stdin resolution, and unknown-cow error handling are already in place from plan 02.

-l columnar shape (D-07, Claude's discretion on exact width — see PATTERNS.md "Shared Patterns: -l Columnar Formatting", lines 529-545):
```
Cow files:
actually  alpaca  beavis.zen  blowfish  bong  bud-frogs  ...
```
Header line `Cow files:` then names joined by a single space, wrapped at ~72-80 columns. Deterministic (ListCows is sorted). Write to the stdout io.Writer, not os.Stdout.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add -l columnar listing, --random selection, and mutual-exclusion guards to run()</name>
  <files>cmd/gosay/main.go</files>
  <read_first>
    - cmd/gosay/main.go (the run() from plan 02 — extend, do not rewrite the stdin/-f logic)
    - .planning/phases/02-input-and-cow-selection/02-PATTERNS.md (main.go run() body steps 2/5/6/7 lines 88-106; -l columnar guidance lines 529-545; --random usage lines 391-399)
    - .planning/phases/02-input-and-cow-selection/02-CONTEXT.md (D-04 all-51 pool, D-05 package rand no seam, D-06 -f+--random conflict, D-07 columnar, D-08 no default marker, D-09 -l conflicts)
    - .planning/phases/02-02-SUMMARY.md (confirm flag scaffold + usage string in place)
  </read_first>
  <action>
    Add `"math/rand"` to the import block. In the FlagSet setup, register `var listFlag bool` via `fs.BoolVar(&listFlag, "l", false, "list available cows")` and `var randomFlag bool` via `fs.BoolVar(&randomFlag, "random", false, "pick a random cow")` (the `flag` stdlib accepts both `-random` and `--random`; no special handling). Extend the `fs.Usage` override string to `"usage: gosay [-f name] [-l] [--random] [message...]"`.

    After `fs.Parse(args)` succeeds, add conflict guards BEFORE any rendering:
    - D-06: if `randomFlag && cowName != "gopher"` (i.e. `-f` was explicitly set alongside `--random`) → `fmt.Fprintln(stderr, "gosay: cannot combine -f and --random")` and return 1. Detect explicit `-f` reliably by checking whether the user set it: use `fsChanged := false; fs.Visit(func(f *flag.Flag){ if f.Name == "f" { fsChanged = true } })` and gate on `randomFlag && fsChanged`. This avoids the false positive where the default "gopher" is not an explicit `-f`.
    - D-09: if `listFlag` AND (`len(fs.Args()) > 0` OR `randomFlag` OR explicit `-f`) → `fmt.Fprintln(stderr, "gosay: -l cannot be combined with a message or animal selection")` and return 1.

    `-l` branch (D-07/D-08): if `listFlag` (and it passed the guard, so it is alone) → `names, err := cowsay.ListCows()`; on err print to stderr return 1. Print `"Cow files:"` line to stdout, then format names into space-separated wrapped lines (accumulate names on the current line; before appending a name, if the current line is non-empty and `len(line)+1+len(name)` would exceed a wrap width constant ~76, flush the line with a trailing newline and start a new one). Write each completed line to the stdout io.Writer. The gopher name appears plain in alphabetical position — do NOT add a `(default)` marker. Return 0. Keep the formatting in a small unexported helper (e.g. `formatCowList(names []string) string`) so it is deterministic and unit-testable; name at Claude's discretion.

    `--random` branch (D-04/D-05): determine the animal AFTER the -l branch and conflict guards. If `randomFlag` → `names, err := cowsay.ListCows()`; on err print stderr return 1; `animal := names[rand.Intn(len(names))]`. Otherwise `animal := cowName`. Use package-level `math/rand` (`rand.Intn`) — NO seeded/injectable RNG (D-05). Then proceed to the existing input-resolution + Render path from plan 02, passing `animal` instead of the literal `cowName`. The pool is the full ListCows() result (all 51, gopher and daemon included) — no exclusion list.

    Preserve all plan-02 behavior: stdin TTY gating, args-win precedence, empty-bubble rendering, and the `errors.Is(err, cowsay.ErrUnknownCow)` clean-error branch must remain intact.
  </action>
  <verify>
    <automated>cd /work/gosay && go build ./... && go vet ./cmd/...</automated>
  </verify>
  <acceptance_criteria>
    - `cd /work/gosay && go build ./...` succeeds.
    - `grep -n 'math/rand' cmd/gosay/main.go` matches; `grep -n 'rand.Intn' cmd/gosay/main.go` matches.
    - `grep -n 'Cow files:' cmd/gosay/main.go` matches.
    - `grep -n 'cannot combine -f and --random' cmd/gosay/main.go` matches.
    - `cd /work/gosay && go run ./cmd/gosay -l | head -1` outputs exactly `Cow files:`.
    - `cd /work/gosay && go run ./cmd/gosay -l | tr ' ' '\n' | grep -c 'gopher'` returns 1 (gopher present, plain).
    - `cd /work/gosay && go run ./cmd/gosay -l | grep -c 'default)'` returns 0 (no (default) marker).
    - `cd /work/gosay && go run ./cmd/gosay -l; echo "exit=$?"` prints `exit=0`.
    - `cd /work/gosay && go run ./cmd/gosay --random hello; echo "exit=$?"` prints `exit=0` and renders a bubble containing `hello`.
    - `cd /work/gosay && go run ./cmd/gosay -f tux --random hello; echo "exit=$?"` prints `exit=1` and stderr contains `cannot combine`.
    - `cd /work/gosay && go run ./cmd/gosay -l hello; echo "exit=$?"` prints `exit=1`.
    - `cd /work/gosay && go vet ./cmd/...` produces no findings.
  </acceptance_criteria>
  <done>-l prints the columnar Cow files: listing (gopher plain, no marker) and exits 0; --random picks from the full 51-cow pool via package rand; -f+--random and -l+input/-selection are hard errors; plan-02 input/error behavior preserved; signatures unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add CLI tests for listing, random membership, and conflict errors</name>
  <files>cmd/gosay/main_test.go</files>
  <read_first>
    - cmd/gosay/main_test.go (existing harness + plan-02 tests to extend)
    - cmd/gosay/main.go (the run() just extended — confirm exact stderr strings and formatCowList helper name)
    - .planning/phases/02-input-and-cow-selection/02-PATTERNS.md (main_test.go sample tests lines 172-243)
  </read_first>
  <action>
    In `cmd/gosay/main_test.go` (package `main`), add tests using the existing `var stdout, stderr bytes.Buffer; code := run(...)` harness:
    - `TestRun_ListFlag_AloneExits0`: `run([]string{"-l"}, ...)` → code 0, stdout first line is `Cow files:` (use `strings.HasPrefix(stdout.String(), "Cow files:")` or split on "\n"), stdout contains `gopher`, stdout does NOT contain `default)`.
    - `TestRun_ListFlag_WithMessage_IsError`: `run([]string{"-l", "hello"}, ...)` → code 1, stdout empty.
    - `TestRun_ListFlag_WithRandom_IsError`: `run([]string{"-l", "--random"}, ...)` → code 1.
    - `TestRun_FlagConflict_FAndRandom`: `run([]string{"-f", "tux", "--random", "hi"}, ...)` → code 1, stderr contains `cannot combine`, stdout empty.
    - `TestRun_Random_ReturnsMember`: `run([]string{"--random", "hello"}, ...)` → code 0, stdout non-empty. (Asserts only membership/validity per D-05, not which animal.)
    If `formatCowList` is an exported-from-package helper in `main`, optionally add a direct unit test asserting its output starts with `Cow files:` and joins names with spaces wrapped under the width; otherwise rely on the run()-level `-l` test. Do not change any plan-02 tests.
  </action>
  <verify>
    <automated>cd /work/gosay && go test ./...</automated>
  </verify>
  <acceptance_criteria>
    - `cd /work/gosay && go test ./cmd/... -v` passes, including TestRun_ListFlag_AloneExits0, TestRun_FlagConflict_FAndRandom, TestRun_Random_ReturnsMember, and the two -l conflict tests.
    - `grep -c 'Cow files:' cmd/gosay/main_test.go` is >= 1.
    - `grep -c 'cannot combine' cmd/gosay/main_test.go` is >= 1.
    - `cd /work/gosay && go test ./...` passes (all packages green).
    - `cd /work/gosay && go vet ./...` produces no findings.
  </acceptance_criteria>
  <done>Tests cover -l alone (exit 0, header + gopher plain), -l conflicts (exit 1), -f+--random conflict (exit 1 + message), and --random membership (exit 0, non-empty); full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `-f` / `--random` / `-l` flags → selection → renderer → terminal | flag values are untrusted input deciding which embedded cow is loaded and printed |
| ListCows() → -l output → terminal | embedded cow names written to the terminal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | -l listing output | accept | `-l` prints only the embedded cow base names (already public, vendored, attributed in cows/NOTICE). No filesystem paths, no internal detail. Names come from sorted ListCows(). Severity: LOW. |
| T-02-06 | Tampering | --random index selection | accept | `rand.Intn(len(names))` is bounded to the ListCows() slice; the chosen name is always a known embedded cow, so the downstream LoadCow cannot hit ErrUnknownCow. No attacker-controlled path. Severity: LOW. |
| T-02-02 | Tampering / EoP | `-f` value (still reachable in this plan) | mitigate | Inherited from plan 02: a traversal `-f` value resolves to fs.ErrNotExist → clean ErrUnknownCow message, no disk access (embed.FS). The D-06 guard additionally blocks `-f`+`--random` ambiguity. Severity: LOW, mitigated. |
| T-02-03 | Information Disclosure | terminal escapes in message content | accept | Unchanged from plan 02: message bytes pass through the balloon verbatim, matching cowsay; no escape interpretation by gosay. Local toy CLI, no trust boundary. Severity: LOW. No HIGH-severity threat in this plan. |
</threat_model>

<verification>
- `go test ./...` passes; `go build ./...` and `go vet ./...` clean.
- Process-level checks (task 1 acceptance criteria) confirm `-l` columnar output, `--random` rendering, and both conflict errors.
- `-l` output is deterministic (ListCows sorted) and contains gopher with no `(default)` marker.
</verification>

<success_criteria>
- `gosay -l` → `Cow files:` header + all 51 names in wrapped columns, gopher plain/alphabetical, exit 0 (COW-03 / D-07 / D-08).
- `gosay --random "hello"` → some embedded animal saying hello, exit 0, animal is a ListCows() member (COW-04 / D-04 / D-05).
- `gosay -f tux --random` → exit 1, `cannot combine` (D-06).
- `gosay -l hello` / `gosay -l --random` / `gosay -l -f tux` → exit 1 (D-09).
</success_criteria>

<output>
Create `.planning/phases/02-input-and-cow-selection/02-03-SUMMARY.md` when done.
</output>
